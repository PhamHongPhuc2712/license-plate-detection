# ─────────────────────────────────────────
#   License Plate Detection - Requirements
# ─────────────────────────────────────────

# Core detection
ultralytics          # YOLOv8 model

# OCR
paddlepaddle==2.6.2  # PaddlePaddle 3.x has an unimplemented oneDNN/PIR bug on CPU
paddleocr==2.7.3     # Last stable 2.x release — uses the classic API (cls=False, nested list results)

# Image & video processing
opencv-python==4.8.1.78  # Last version compatible with numpy 1.x
numpy==1.26.4            # numpy 2.x breaks PaddlePaddle

# Config
pyyaml               # Load config.yaml

# Utilities
pandas               # Optional: for reading/analyzing detections.csv